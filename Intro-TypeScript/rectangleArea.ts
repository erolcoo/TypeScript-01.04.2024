function calculateRectangleArea(len: number, width: number) :number {
    return length * width;
}

const len= 5;
const width = 7;
const area = calculateRectangleArea(length, width);
console.log(area); 
